const fact = (factor) =>
    factor = 2; factor * factor <= num; factor++
    {
        while (num % factor == 0) {
            console.log(factor + " ");
            num = num / factor;
        }}
module.exports = {
    fact,

}